//Compile different stylings into a theme object here.
//Values should be comma separated rather than semi-colon separated. 
//To pull them, import this file and then use template literals.
//${props => props.theme.primaryColor}

export default {
    primaryColor: 'red',
    secondaryColor: 'blue',
};